import 'package:flutter/material.dart';

class AniversariantesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tela de Aniversariantes'),
    );
  }
}
